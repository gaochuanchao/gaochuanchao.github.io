# Gao Chuanchao Personal Website

Source for <https://gaochuanchao.github.io/>.

The site is built with Jekyll and GitHub Pages. Main content pages live in `_pages/`, navigation is configured in `_data/navigation.yml`, and PDFs are stored in `papers/`.
